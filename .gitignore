# Python
    __pycache__/
    *.py[cod]
    *$py.class
    *.egg-info/

    # Virtual environment
    venv/
    .venv/
    ENV/
    env/
    VENV/
    # Add any other venv names

    # IDE / Editor specific
    .vscode/
    .idea/
    *.sublime-project
    *.sublime-workspace
    *.komodoproject/
    nbproject/ # Netbeans
    *.swp
    *.swo

    # Secrets - NEVER COMMIT THESE
    .env*
    *.env
    config.py # If it contains secrets or is truly unused

    # Application output / temporary files
    media/
    final_lesson.mp4
    slide_*.py
    list.txt
    video_requests.json # Runtime state
    *.mp4 # Other temporary videos
    *.log # Log files

    # OS specific
    .DS_Store
    Thumbs.db
    Desktop.ini

    # Manim specific temp files (if any others are common)
    Tex/ # Manim's default Tex directory
    text_files/ # Manim's default text_files directory
    sound_files/ # Manim's default sound_files directory